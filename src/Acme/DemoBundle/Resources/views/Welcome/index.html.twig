{% extends 'AcmeDemoBundle::layout.html.twig' %}

{% block title %}Code Review{% endblock %}

{% block content_header '' %}

{% block content %}
    {% set version = constant('Symfony\\Component\\HttpKernel\\Kernel::MAJOR_VERSION') ~ '.' ~ constant('Symfony\\Component\\HttpKernel\\Kernel::MINOR_VERSION')%}

    <h1 class="title">The Code Review</h1>

    <p>Welcome. This Action writes only <a href="http://en.wikipedia.org/wiki/Prime_number" target="_blank">prime numbers</a> from {{ min }} to {{ max }}. So, you have to test PrimeManager</p>

    <p>at `src/Acme/DemoBundle/Manager/PrimeManager.php`</p><br><br><br>

    <div>
        {% for number in numbers %}
            <span style="background: #ddd;padding: 5px 10px;display: inline-block;margin: 5px;">{{ number }}</span>
        {% else %}
            <span style="background: #ddd;padding: 5px 10px;display: inline-block;margin: 5px;">There is no prime numbers!</span>
        {% endfor %}
    </div>

{% endblock %}
